{
  "name": "pubnebula",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "fetch:data": "node scripts/fetch-openalex.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "test": "vitest run",
    "build": "npm run validate:data && tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "verify:visual": "node scripts/verify-visual.mjs"
  },
  "dependencies": {
    "three": "^0.179.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "playwright-core": "^1.60.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4",
    "playwright-core": "^1.57.0"
  }
}
